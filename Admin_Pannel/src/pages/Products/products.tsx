import ProductsList from "components/sections/Product/Product";
import {ReactElement} from "react";

const products = (): ReactElement => {
    return (
        <ProductsList />
    );
};

export default products;